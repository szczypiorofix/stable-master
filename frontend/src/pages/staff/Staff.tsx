import { JSX } from 'react';
import { Box } from '@mui/material';

export function Staff(): JSX.Element {
    return (
        <Box pt={2} pb={2}>
            This is Staff page
        </Box>
    );
}
